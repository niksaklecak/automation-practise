import { test } from "@playwright/test";

test.describe("Book a Demo", () => {
  test.fixme("navigation link works", async () => {});
  test.fixme("all form fields are present", async () => {});
  test.fixme("form validation works correctly", async () => {});
});
